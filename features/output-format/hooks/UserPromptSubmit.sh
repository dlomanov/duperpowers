#!/usr/bin/env bash
# Personal reminder, every turn: reply using the output-format skill.
# Plain text for the feature-hooks dispatcher (it wraps this in the JSON envelope).
printf '%s\n' "Reply using the output-format skill."
